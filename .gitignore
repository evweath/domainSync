# Python
.venv/
__pycache__/
*.py[cod]
*.pyo

# SSL certs (generated locally)
certs/*.pem

# Database and WAL files
data/*.db
data/*.db-shm
data/*.db-wal
data/*.db.bak-*
data/*.bak-*

# Logs and exports
logs/*.log
exports/

# Persisted store scan snapshots (runtime cache, 5-day TTL)
data/scan_cache/

# macOS
.DS_Store

# Local config with secrets (API keys, credentials)
config/settings.yaml

# Generated Windows distribution artifacts (rebuild via create_windows_package.py)
DonutIntel-Windows.zip
WINDOWS-SETUP-GUIDE.pdf
_guide_tmp.html
